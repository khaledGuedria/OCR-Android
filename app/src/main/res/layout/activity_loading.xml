<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#e3e3e3"
        tools:context=".MainActivity">

    <ImageView
            android:layout_width="300dp"
            android:layout_height="300dp"
            app:srcCompat="@drawable/gifanim"
            android:id="@+id/loaderImg"
            android:layout_marginTop="92dp"
            app:layout_constraintTop_toTopOf="parent" app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginEnd="56dp"/>
    <TextView
            android:text="Scan."
            android:layout_width="349dp"
            android:layout_height="68dp"
            android:id="@+id/textView"
            android:textColor="#000"
            android:textAlignment="center"
            android:textSize="42sp"
            android:layout_marginStart="24dp"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="72dp"
            app:layout_constraintTop_toBottomOf="@+id/loaderImg"/>
    <TextView
            android:text="Please wait until the scanning is over."
            android:layout_width="348dp"
            android:layout_height="40dp"
            android:id="@+id/loaderText2"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@+id/textView"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="28dp"
            android:textAlignment="center"/>

    <ProgressBar
            style="@android:style/Widget.Material.Light.ProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/progressBar"
            android:indeterminateTint="@color/myGreen"
            android:progressDrawable="@drawable/circularprogressbar"
            android:layout_marginTop="64dp"
            app:layout_constraintTop_toBottomOf="@+id/loaderText2" android:layout_marginStart="180dp"
            app:layout_constraintStart_toStartOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>